pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // આ લાઈન મહત્વની છે
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "indian-calendar-app"
include ":app"

